{
  "name": "solana-grid-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone grid-trading bot for Solana tokens via Jupiter's Trigger API. Rebuilt from the grid module of solana-scalpe-bot-alpha after that project directory was accidentally deleted.",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "grid": "node --env-file-if-exists=.env src/cli/grid.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4",
    "bs58": "^6.0.0"
  }
}
